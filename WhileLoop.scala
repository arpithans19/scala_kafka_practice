package com.basic

object WhileLoop {
  def main(args: Array[String]) {
   var a = 1; // Initialization

    while (true) { //infinite while
//       while( a<=10 ){        // Condition
      println(a);
      a += 1 // Incrementation
    }
  }
}